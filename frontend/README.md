# GFW Check Frontend

React webapp for checking if websites are blocked by the Great Firewall of China.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the development server:
   ```bash
   npm start
   ```

The app will open at `http://localhost:3000`

## Build

```bash
npm run build
```

This creates an optimized production build in the `build/` folder.

## Environment Variables

Create a `.env` file in the root directory:

```
REACT_APP_API_URL=http://your-api-server:8000
```

## Features

- Simple URL input
- Real-time blocking status from backend
- Clean, responsive UI



THIS README IS GENERATED BY GITHUB COPILOT